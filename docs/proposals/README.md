# Proposals

This directory contains roadmaps, migration plans, unfinished refactors, and
capabilities under evaluation.

Every document must include `Status: Proposed` and must not describe
unimplemented behavior as something the current system already does. Proposals
are outside the default AI reading path.
