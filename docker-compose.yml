name: openexcel

services:
  server:
    image: ${OPENEXCEL_IMAGE:-openexcel:local}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openexcel-server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
      - DATABASE_PROVIDER=sqlite
    volumes:
      - openexcel-data:/app/.data

volumes:
  openexcel-data:
